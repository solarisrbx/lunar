--[[
    Lunar by Solaris

    Da Hood Script
    0.0.1
]]--

local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/solarisrbx/lunar/dev/util/lib.lua"))()

local hood = {}
hood.gui = library.new("Lunar")

hood:Notify("Injected", "Detected we are in Project Exchange")